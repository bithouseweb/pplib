// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pplib", "for pplib support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pplib", "enable pplib support", "no");

if (PHP_PPLIB != "no") {
	EXTENSION("pplib", "pplib.c");
}

